#include <stdio.h>

/* This is a comment. */
int main(int argc, char *argv[])
{
	int distance = 100; // variable declaration (type variable name then the value)

	// this is also a comment
	printf("You are %d miles away.\n", distance);

	return 0;
}
